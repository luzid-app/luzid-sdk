{
  "name": "@luzid/build-scripts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'We do not build the build-scripts'",
    "compile": "echo 'We do not compile the build-scripts'"
  },
  "files": [
    "add-js-extension-to-types.mjs",
    "env-shim.ts",
    "tsup.config.library.ts",
    "tsup.config.package.ts"
  ],
  "devDependencies": {
    "@types/node": "18.11.19",
    "browserslist-to-esbuild": "^1.2.0"
  }
}
